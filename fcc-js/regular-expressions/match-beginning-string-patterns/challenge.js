// Solve the challenge in this JavaScript file
const rickyAndCal = 'Cal and Ricky both like racing.';
const calRegex = /^Cal/; // Change this line
const result = calRegex.test(rickyAndCal);

console.log(result);
